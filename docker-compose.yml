version: "3.8"
services:
  app:
    container_name: college-hack-api
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=dev
    depends_on:
      - mongo
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
